<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Drivo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:fontFamily">@font/urbanist_regular</item>
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>

        <!-- Ensure text input fields have visible text in dark mode -->
        <item name="android:editTextColor">@color/input_text</item>
        <item name="android:textColorHint">@color/input_hint</item>

        <!-- Fix Material TextInputLayout colors for dark mode -->
        <item name="colorOnSurface">@color/onBackground</item>
        <item name="colorOnSurfaceVariant">@color/input_hint</item>
    </style>

    <style name="Widget.App.BottomNavigationView"
        parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemBackground">@null</item>
        <item name="itemHorizontalTranslationEnabled">false</item>
        <item name="android:background">@null</item>
    </style>
</resources>